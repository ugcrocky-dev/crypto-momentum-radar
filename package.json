{
  "name": "crypto-momentum-radar",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/prepare_static.js",
    "test": "node --test tests/*.test.js",
    "start": "npx serve public -p 3000",
    "validation": "node scripts/validation_forward.js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
