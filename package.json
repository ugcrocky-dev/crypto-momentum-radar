{
  "name": "crypto-momentum-radar",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/prepare_static.js",
    "test": "node --test tests/freshness.test.js",
    "start": "npx serve public -p 3000"
  }
}
