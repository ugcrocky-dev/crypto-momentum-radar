# Deploy status

Production deploy via Vercel MCP `deploy_to_vercel`.

- `/api/momentum` — peer recovery + `btcRelative` enrich
- `/api/high-frequency` — independent freshness
- Research APIs (`early-setups`, `ohlcv`) rolled after core restore
- Homepage includes `freshness-guard.js`

See RESEARCH.md and FRESHNESS.md.
